import styled from 'styled-components';
import { themeGet } from '@styled-system/theme-get';

export const ViewHistoryWrapper = styled.div`
    cursor: pointer;
    svg {
        width: 24px;
        margin-left: 8px;
        g, path {
            fill: #fff;
        }
    }
`;
